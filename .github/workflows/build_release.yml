name: Build Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      enable-cache: false
      release-build: true
      package-artifacts: true
      upload-to-release: true
