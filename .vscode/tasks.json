{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BuildCompiler",
            "command": "cargo build",
            "type": "shell",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/infc"
            },
            "group": "build"
        }
    ]
}